# RabbitMQ configuration to permit deprecated transient_nonexcl_queues
# This allows older clients (kombu/amqp) to declare transient non-exclusive queues.
# Remove once clients/libraries are upgraded and no longer use this feature.
deprecated_features.permit.transient_nonexcl_queues = true
